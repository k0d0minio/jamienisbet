# Handoff: inbox-rebuild

For the next session — human or agent — what to do first and what stands in the way.
Rewritten, not appended, at every stage stop; a stage that STOPs mid-way writes it before it
stops, so nothing is carried in anyone's head.

## Next steps

1. Operator smokes the jamie-nisbet preview of https://github.com/k0d0minio/jamienisbet/pull/174
   at `/inbox` (desk ≥1024px and a phone): the kind table, e/s/↵/j/k, log a touch → accept the
   step clears the row, a wake returns as an outreach row, the badge equals the row count.
2. Once **Ready to merge** is ticked: `/pipeline release inbox-rebuild`.

## Blockers

- blocked on operator: smoke the preview and tick **Ready to merge** in the body of https://github.com/k0d0minio/jamienisbet/pull/174

## Do not

- Do not tick either gate box.
- Do not add the Gates and PRs group or any GitHub read to `/inbox` — that is `gates-read`.
- Do not restyle `lead-row.tsx` / `swipe-row.tsx` for the desk tier here — `retire-app-tier` owns them.
