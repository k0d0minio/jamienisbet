# Build notes: drop-todos-compliance

- commits: 09deca2 admin surface · 1365e42 schema + migration 0026 · 4511d18 docs
- ci: draft head — lint.sh OK, security-check.sh OK; full verdict settled after the ready flip (below)

## What changed

- `websites/admin-dashboard/app/(app)/page.tsx`: the feed no longer reads tasks or compliance
  dates; Overdue section, compliance calendar and the `AddTodo` title-bar action gone; the
  "N things waiting on you" sum drops both terms. `COMPLIANCE_HORIZON_DAYS` and `DAY_MS` went with
  their only uses.
- `app/(app)/leads/[id]/page.tsx`: `listOpenTasksForClient` out of the parallel read, the
  `LeadTodos` card out of the Work segment.
- `app/(app)/actions.ts`: the Todos and Compliance calendar action blocks and their eight imports.
- `components/{overdue-list,add-todo,compliance-calendar,lead-todos}.tsx`: deleted.
- `app/(app)/loading.tsx`: the feed skeleton's second section is now "Outreach due" instead of
  "Overdue", so the skeleton no longer promises a section that cannot appear.
- Comments naming todos/Overdue in `leads/page.tsx`, `leads/[id]/loading.tsx`,
  `lead-segments.tsx`, `nurture-wakes.tsx`: corrected.
- `packages/services`: `tasks` and `complianceDates` out of `src/schema/index.ts` (the "two working
  lists" header went with them); `queries/tasks.ts` and `queries/compliance.ts` deleted with their
  barrel re-exports.
- `packages/services/drizzle/0026_drop_tasks_and_compliance_dates.sql` (+ `meta/0026_snapshot.json`,
  journal entry): generated by `drizzle-kit generate --name …`, then given the same header comment
  0025 carries. Two statements: `DROP TABLE "biz"."compliance_dates" CASCADE;` and
  `DROP TABLE "biz"."tasks" CASCADE;` — nothing references either table.
- Docs: `websites/admin-dashboard/README.md`, `packages/services/README.md`, `AGENTS.md` (the
  routing row is gone) — and, beyond the spec's `touches:`, one-line fixes to `README.md`,
  `packages/README.md` and `websites/README.md`, which also listed todos/compliance as live.

## Acceptance criteria status

- [x] Feed shows no Overdue section, no compliance row/sheet, no add-todo button — removed from `page.tsx`.
- [x] "N things waiting" no longer counts todos or compliance — both terms removed from `count`.
- [x] Lead profile shows no Todos card; the other cards unchanged — only `LeadTodos` and its read removed.
- [x] `packages/services` exports no task/compliance query, table or type; `git grep` for the named symbols finds nothing outside migrations.
- [x] The four components and the todo/compliance server actions no longer exist.
- [x] One new migration drops both tables and the journal lists it — `Validate migrations (no DB writes)` reads it on the ready head.
- [ ] `Apply migrations to production` green on `main` — only after the merge; Release reads it.
- [x] The READMEs and `AGENTS.md` no longer describe todos, Overdue or compliance as live features.

## Notes for Release

- **Forward-only, no revert path.** Once 0026 runs on `main`, reverting this PR redeploys code that
  queries dropped tables. Recovery is fix-forward (`hotfix`). Record it on the `- migrations:` line.
- **Merge window.** `db-migrations.yml` drops the tables on the push to `main` while Vercel is still
  building; until the new admin deployment is live, the old one's `/` and `/leads/<id>` error.
  Smoke both once the production deployment is ready.
- The preview runs against the shared production database (`database.isolation: none`), which
  still has both tables — the preview proves the code no longer reads them, not the migration.
- Three READMEs outside `touches:` were corrected (see above) — docs only, no behaviour.
