# Handoff: shell-rail-palette

For the next session — human or agent — what to do first and what stands in the way.
Rewritten, not appended, at every stage stop; a stage that STOPs mid-way writes it before it
stops, so nothing is carried in anyone's head.

## Next steps

1. Merged and archived; nothing to pick up. The next stub in the batch is `work-panes` (`/pipeline new`).

## Blockers

- none

## Do not

- Do not reopen or re-run this run; follow-ups are the two triage stubs it parked (`inbox-badge-live-on-navigation`, `inbox-count-without-full-client-read`).
