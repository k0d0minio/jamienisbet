# Handoff: master-detail-shell

For the next session — human or agent — what to do first and what stands in the way.
Rewritten, not appended, at every stage stop; a stage that STOPs mid-way writes it before it
stops, so nothing is carried in anyone's head.

## Next steps

1. Merged and archived; nothing to pick up. The next stubs in the epic are `ticket-view`,
   `epic-view` and `repo-and-estate-views` (independent of each other), then `keyboard-nav`.

## Blockers

- none

## Do not

- Do not reopen this run; follow-ups are new stubs.
