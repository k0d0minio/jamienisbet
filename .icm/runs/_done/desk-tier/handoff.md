# Handoff: desk-tier

Merged and archived; nothing to pick up. The next stub in the batch that builds on it is
`shell-rail-palette` (it links `desk.css` into the admin and gives the primitives their first
rendering surface).
