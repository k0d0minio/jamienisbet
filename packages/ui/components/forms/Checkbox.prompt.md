Checkbox with a custom slate check and optional description.

```jsx
<Checkbox label="Email me the proposal" defaultChecked />
<Checkbox label="NDA required" description="I'll send one before our first call." />
```

Spreads native input props (`checked`, `onChange`, `disabled`).
