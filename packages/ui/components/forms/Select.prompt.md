Brand-styled native select.

```jsx
<Select label="Engagement" placeholder="Choose one"
  options={["Advisory retainer", "Fixed-scope build", "Fractional CTO"]} />
```

Pass `options` as strings or `{value,label}`. Spreads native select props (`value`, `onChange`).
