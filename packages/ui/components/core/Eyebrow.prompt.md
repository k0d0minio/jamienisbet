The brand's signature mono kicker — uppercase IBM Plex Mono with wide tracking. Use above section titles and headings.

```jsx
<Eyebrow rule>Selected work</Eyebrow>
<Eyebrow primary index="01">Discovery</Eyebrow>
```

Use `rule` for a short leading line, `primary` to tint slate, `index` for numbered sections.
