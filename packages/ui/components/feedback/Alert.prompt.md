Inline banner for status, notices and confirmations.

```jsx
<Alert variant="success" title="Brief received">I'll reply within two working days.</Alert>
<Alert variant="warning">This proposal expires on 30 June.</Alert>
```

Variants `info` / `success` / `warning` / `danger` each carry a default icon you can override with `icon`.
