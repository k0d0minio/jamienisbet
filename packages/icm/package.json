{
  "name": "@jamie-nisbet/icm",
  "version": "0.1.0",
  "private": true,
  "description": "Jamie Nisbet ICM runtime — loads Layer-3 context (stage contracts, templates, brand voice, rates) from the repo and assembles per-stage generation context for the admin dashboard's AI pipeline. Server-only, source-only, no build step.",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3"
  }
}
